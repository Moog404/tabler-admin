{% extends 'base.admin.html.twig' %}

{% block title %}Edit Machine{% endblock %}

{% block body %}
    <a href="{{ path('back_machine_index') }}">back to list</a>

    <div class="container-xl">
        <!-- Page title -->
        <div class="page-header d-print-none mt-4 mb-4">
            <div class="row align-items-center">
                <div class="col">
                    <h2 class="page-title">
                        Title
                    </h2>
                    <div class="page-pretitle">
                        subtitle
                    </div>
                </div>
            </div>
        </div>
    </div>

<div class="page-body">
    <div class="container-xl">
        <div class="row row-deck row-cards">
            {{ include('backend/machine/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
            {{ include('backend/machine/_delete_form.html.twig') }}
        </div>
    </div>
</div>

{% endblock %}
